# AgentDesk 필요 기능 분석

> 프로젝트 코드베이스·문서·계획을 기반으로 정리한 **현재 필요한 기능** 목록입니다.
> 최종 갱신: 2026-03-06 (코드베이스 실사 + P1 전체 구현 완료 + P2 전체 구현 완료 + OpenClaw 경쟁 분석 반영)

---

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| **이름** | AgentDesk |
| **정의** | AI 에이전트 오피스 시뮬레이터 — CEO 데스크에서 AI 에이전트 팀을 지휘 |
| **버전** | 2.0.1 |
| **스택** | Node ≥22, pnpm ≥10, Express 5, React 19, Vite, SQLite, Electron(Windows exe) |

---

## 2. 이미 구현된 핵심 기능

코드베이스 실사 기준으로 **실제 동작 확인된** 기능입니다.

| 영역 | 기능 | 구현 위치 | 비고 |
|------|------|-----------|------|
| **오피스/대시보드** | 픽셀아트 오피스 뷰, 부서별 에이전트 이동 | `OfficeView`, Pixi.js | 스프라이트, 룸 테마 커스텀 |
| | CEO 대시보드(KPI, 에이전트 랭킹, 부서 성과, 미션 로그) | `Dashboard.tsx`, `dashboard/` | HeroSections, OpsSections, CollapsibleSection |
| **태스크** | 칸반 보드(Inbox → Planned → In Progress → Review → Done 등) | `TaskBoard.tsx`, `taskboard/` | DnD, FilterBar, BulkHideModal |
| | 태스크 할당·실행·터미널 로그·diff·merge/discard | API + UI | DiffModal, TerminalPanel |
| | 서브태스크, worktree 기반 격리 | `server/modules/workflow/core/worktree/` | Git 있으면 worktree, 없으면 direct |
| | 태스크 실행 세션·중단·재개·일시정지 | `execution-control.ts`, `execution-run.ts` | pause/resume/stop API |
| **결과물** | Deliverables 전용 화면 | `deliverables/Deliverables.tsx` | DeliverableCard, ArtifactList, GitSection, TextPreviewModal, CollaboratorSection |
| | 결과물 상태 필터(all/done/review) | `Deliverables.tsx` | 구현 완료 |
| | 산출물 파일 메신저 전송 | `gateway/client.ts` (`sendDeliverableFiles`) | Telegram 파일 전송 지원 |
| **에이전트/부서** | 다중 CLI 프로바이더(Claude, Codex, Gemini, Copilot, Antigravity, Cursor, API) | `spawn.ts`, `api-providers.ts` | OAuth, API Provider 지원 |
| | 부서·에이전트 CRUD, 스폰·상태 | `agents/crud.ts`, `departments.ts` | 에이전트 폼 모달, 부서 폼 모달 |
| | AI 캐릭터 페르소나 자동 생성 | `AgentFormModal.tsx`, `POST /api/agents/generate-persona` | 이름 입력 → AI 페르소나 생성 |
| | 캐릭터 페르소나 프롬프트 주입 (7개 지점) | `character-persona.ts` | spawn, execution-run, execution-start-task, subtask-delegation, cross-dept, meeting, direct-chat |
| **메신저** | Telegram, Discord, Slack, WhatsApp, Google Chat, Signal, iMessage | `gateway/client.ts` | 채널별 전송·타이핑 인디케이터 |
| | 메신저 세션 관리·토큰 암호화 | `GatewaySettingsTab.tsx`, `token-crypto.ts` | 세션별 개별 토큰, 에이전트 바인딩 |
| | Inbox 웹훅(`POST /api/inbox`), CEO 지시(`$`), 태스크 등록(`#`) | `directives-inbox-routes.ts` | 오케스트레이션 |
| **프로젝트** | 프로젝트 CRUD, 경로 검증·제안·브라우즈·네이티브 피커 | `projects.ts`, `ProjectManagerModal.tsx` | ProjectSidebar, ProjectEditorPanel, ProjectInsightsPanel |
| | GitHub 리포 임포트 (Device Flow OAuth) | `GitHubImportPanel.tsx`, `github-routes.ts` | GitHubDeviceConnect, GitHubImportWizard |
| **라이브러리** | 스킬 학습·이력·카드·카테고리 | `SkillsLibrary`, `skills-library/` | ClassroomOverlay, LearningModal, CustomSkillModal |
| | 에이전트 룰 CRUD·학습·이력 | `AgentRulesLibrary`, `agent-rules/` | RuleFormModal, RuleHistoryPanel, RuleMemorySection |
| | 메모리 CRUD·학습·이력 | `MemoryLibrary`, `memory/` | MemoryFormModal, MemoryLearningModal, MemoryHistoryPanel |
| | 훅 CRUD·학습·이력 | `HooksLibrary`, `hooks/` | HookFormModal, HookHistoryPanel |
| **의사결정** | Decision Inbox API·UI (project_review_ready, task_timeout_resume, review_round_pick) | `DecisionInboxModal.tsx`, `decision-inbox-routes.ts` | 자동 YOLO 모드 포함 |
| **협업** | CEO 미팅(planned/review), 서브태스크 위임, 크로스부서 협업 | `meeting-prompt-tools.ts`, `subtask-delegation-prompt.ts`, `cross-dept-cooperation.ts` | 회의록, 참여자 관리 |
| | 에이전트 다이렉트 챗 (프로젝트 바인딩, 태스크 플로우) | `direct-chat.ts`, `ChatPanel.tsx` | ProjectFlowDialog, ChatModeHint |
| **설정** | 일반·CLI·API·OAuth·Gateway 설정 탭 | `SettingsPanel.tsx`, `settings/` | 6개 탭: General, CLI, API, OAuth, Gateway, GitHub |
| **Workflow Pack** | 팩 정의·라우팅·실행 분기 (development, novel, report, video_preprod, web_research_report, roleplay, asset_management) | `workflow/packs/` | DB 테이블, 오피스팩 부서/에이전트 자동 생성 |
| | 오피스 팩 UI 전환·부서/에이전트 시드 | `AppMainLayout.tsx`, `office-workflow-pack.ts` | 팩별 부서·에이전트·룸 테마 |
| | 문서 생성 파이프라인 가이던스 (PPT/Excel/PDF/Word) | `ppt-execution-guidance.ts` | 키워드 감지 → 에이전트 프롬프트 주입 |
| | 비디오 프리프로덕션 (Remotion) | `video-artifact.ts`, `video-render-engine-gate.ts`, `video-skill-bootstrap.ts` | 자동 스킬 설치, 렌더 엔진 검증 |
| **기타** | 다국어(ko/en/ja/zh) | `i18n.ts` | 전체 UI + 프롬프트 |
| | 자동 업데이트 알림·적용 | `update-auto/` | exe 자동 업데이트 |
| | EXE 패키징 (portable/nsis) | `electron/`, `exe-packaging-plan.md` | 구현 완료 |
| | 태스크 보고서·리포트 아카이브 | `task-reports/routes.ts`, `TaskReportPopup.tsx`, `ReportHistory.tsx` | 요약·산출물·Git 변경 |
| | 에이전트 상태 패널 | `AgentStatusPanel.tsx` | 실시간 상태 모니터링 |
| | CLI 사용량 패널 | `CliUsagePanel.tsx` | 프로바이더별 사용량 |

---

## 3. 구현 상태 vs 문서/계획 비교

### 3.1 결과물(Deliverables) 화면 ✅ 구현 완료

| 항목 | 내용 |
|------|------|
| **출처** | `docs/deliverables-spec.md` |
| **상태** | **구현 완료.** 전용 라우트(`tasks-deliverables`), DeliverableCard(접기/펼치기), 결과 요약, 협업 참여자 섹션, 산출물 파일 목록, Git 변경 섹션, 텍스트 미리보기 모달, 상태 필터 모두 구현됨. |
| **보완 가능** | • 파일 유형별 미리보기 확장 (이미지·PDF 인라인 뷰어) — 다운로드는 ✅ 구현 완료 |

### 3.2 에이전트 룰 학습 UI ✅ 구현 완료

| 항목 | 내용 |
|------|------|
| **출처** | `docs/agent-rules-learning-ui-analysis.md` |
| **상태** | **구현 완료.** 학습 job·이력·카드, RuleFormModal, RuleHistoryPanel, RuleMemorySection 모두 구현됨. 메모리·훅도 동일 패턴. **낙관적 업데이트** 3개 라이브러리 모두 구현 (Agent Rules 기존 + Memory/Hooks 추가). |
| **보완 가능** | • 카드에서 "학습됨" 상태일 때 버튼 문구/스타일 구분 개선 |

### 3.3 Workflow Pack ✅ MVP 구현 완료

| 항목 | 내용 |
|------|------|
| **출처** | `docs/plans/2026-02-27-workflow-pack-mvp.md` |
| **상태** | **MVP 구현 완료.** `workflow_packs` DB 테이블, 7개 팩 정의(development, novel, report, video_preprod, web_research_report, roleplay, asset_management), 팩별 실행 분기, 오피스팩 UI 전환, 부서/에이전트 자동 시드, 태스크 팩 필터링 모두 동작. |
| **보완 가능** | • ~~팩별 필수 입력 수집 폼~~ ✅ 구현 완료 (`CreateTaskModalView`에서 `inputSchema` 기반 동적 폼 렌더링, `workflow_meta_json`으로 저장)<br>• ~~팩별 QA 게이트 자동 적용~~ ✅ **구현 완료** (2026-03-06). `execution-guidance.ts`에서 `qa_rules_json` 기반 다국어 프롬프트 주입 + `run-complete-handler.ts`에서 완료 시 QA 규칙 경고 로그 자동 생성. development(테스트·리스크), report(필수섹션), web_research_report(인용), novel(톤·캐릭터), asset_management(리스크·컴플라이언스·성과지표), roleplay(캐릭터보이스·안전정책) 팩 지원.<br>• ✅ 세션/메신저별 기본 팩 자동 라우팅 UI 노출 완료. 프로젝트 편집기에 `default_pack_key` 선택 드롭다운 추가. Gateway 설정 세션별 팩 선택 이미 구현. |

### 3.4 의사결정 Inbox · 메신저 Wake 알림 ✅ 구현 완료

| 항목 | 내용 |
|------|------|
| **출처** | `docs/decision-inbox-checkpoint-2026-02-22.md` |
| **상태** | **구현 완료.** Decision Inbox API·UI·YOLO 모드 + 메신저 wake 알림 연동. `notifyDecisionInbox()` → `queueWake()` 패턴으로 CEO 메신저에 알림 전송. `lastWakeItemCount` 추적으로 중복 방지. |
| **구현 위치** | `gateway/client.ts` (`notifyDecisionInbox`), `decision-inbox-routes.ts` (`getDecisionInboxItems` 내 호출) |

### 3.5 EXE 패키징 ✅ 구현 완료

| 항목 | 내용 |
|------|------|
| **출처** | `docs/exe-packaging-plan.md` |
| **상태** | **구현 완료.** portable/nsis exe 빌드, 자동 업데이트 알림·적용 시스템 포함. |

---

## 4. 코드베이스 실사에서 발견된 추가 필요 기능

문서에 명시되지 않았지만, 코드 분석에서 발견된 부족 사항입니다.

### 4.1 산출물 파일 다운로드 ✅ 이미 구현 확인

| 항목 | 내용 |
|------|------|
| **현황** | **구현 완료.** `GET /api/task-reports/:taskId/artifacts/download` 다운로드 엔드포인트(zip 다운로드) 존재. `ArtifactList.tsx`에서 다운로드 버튼(`getTaskArtifactDownloadUrl`) 연동 완료. |
| **보완 가능** | ✅ 구현 완료. 이미지(png/jpg/gif/svg/webp) 인라인 미리보기 + PDF iframe 뷰어. `ArtifactList.tsx`에서 토글 기반 expand/collapse 지원. |

### 4.2 에이전트 성과 분석·히스토리 ✅ MVP 구현 완료

| 항목 | 내용 |
|------|------|
| **현황** | **MVP 구현 완료.** AgentDetail에 "Performance" 탭 추가. `GET /api/agents/:id/performance` 엔드포인트에서 전체/완료/실패 태스크 수, 성공률, 평균 소요시간, 팩별 실적, 최근 태스크 이력 제공. `AgentPerformancePanel.tsx`에서 시각화. |
| **구현 위치** | API: `agents/crud.ts`, 프론트: `agent-detail/AgentPerformancePanel.tsx`, `AgentDetailTabContent.tsx`, `AgentDetail.tsx` |
| **보완 가능** | • 에이전트별 토큰 비용 추적 (현재 프로바이더 레벨만 지원)<br>• 시간축 추이 차트 (일별/주별)<br>• 프로바이더별 성공/실패 분석 |

### 4.3 태스크 실행 로그 검색·필터 ✅ 구현 완료

| 항목 | 내용 |
|------|------|
| **현황** | **구현 완료.** `TerminalPanel.tsx`에 검색 바(돋보기 토글), 로그 종류 필터(all/system/error), 검색 하이라이트(yellow mark), 복사 버튼, .log 다운로드 버튼 추가. |
| **구현 위치** | `TerminalPanel.tsx`: `highlightSearchMatches`, `logSearch`, `logKindFilter`, `handleCopyLog`, `handleDownloadLog` |

### 4.4 프로젝트 대시보드 강화 ✅ 진행률 구현 완료

| 항목 | 내용 |
|------|------|
| **현황** | **프로젝트 진행률 구현 완료.** `ProjectInsightsPanel`에 `ProjectProgressSection` 추가. 완료율 프로그레스 바, 상태별 배지(완료/진행중/리뷰/일시정지/예정/실패), 에이전트별 기여도(상위 4명) 시각화. |
| **구현 위치** | `project-manager/ProjectInsightsPanel.tsx` (`ProjectProgressSection` 내장 컴포넌트) |
| **보완 가능** | • 시간축 기반 번다운/번업 차트 (태스크 완료 타임스탬프 집계 필요)<br>• 부서별 기여도 분류 |

### 4.5 알림 센터 (Notification Center) ✅ 구현 완료

| 항목 | 내용 |
|------|------|
| **현황** | **구현 완료.** `notifications` DB 테이블, REST API (`GET/POST/DELETE /api/notifications`), WebSocket 실시간 브로드캐스트(`"notification"` 이벤트), 프론트엔드 `NotificationCenter.tsx` (벨 아이콘 + 미읽기 배지 + 드롭다운 패널). 태스크 완료(`task_complete`)·실패(`task_error`) 시 자동 알림 생성. |
| **구현 위치** | DB: `base-schema.ts`, API: `ops/notifications.ts`, 프론트: `NotificationCenter.tsx`, `AppHeaderBar.tsx`, 라이프사이클 연동: `report-workflow-tools.ts`, `review-finalize-tools.ts`, `run-complete-handler.ts` |
| **보완 가능** | • 브라우저 Push Notification (Notification API)<br>• 알림 유형별 필터 |

### 4.6 태스크 의존성·간트 차트 ✅ 의존성 + 간트 구현 완료

| 항목 | 내용 |
|------|------|
| **현황** | **의존성 + 간트 차트 구현 완료 (2026-03-06).** |
| **구현 내용 (의존성)** | `task_dependencies` DB 테이블, REST API (GET/POST/DELETE `/api/tasks/:id/dependencies`), TaskCard에 선행 태스크 인라인 UI(추가·삭제·목록). 순환 의존성 방지 로직 포함. |
| **구현 내용 (간트 차트)** | TaskBoard 내 Board/Gantt 뷰 전환 토글. 줌 레벨(일/주/월), 상태별 색상 바, 오늘 마커, 에이전트 아바타, 상태/부서 필터, 범례, 툴팁 (시작/종료일, 상태). |
| **구현 위치** | DB: `base-schema.ts`, API: `core/task-dependencies.ts`, 프론트: `taskboard/GanttChart.tsx`, `TaskBoard.tsx`, `api/task-dependencies.ts` |
| **보완 가능** | • 크리티컬 패스 하이라이트<br>• ✅ 의존성 완료 전 실행 자동 차단 구현 완료 (`startTaskExecutionForAgent`에서 선행 태스크 검사 + CEO 알림)<br>• 간트 차트에 의존성 화살표 표시 |

### 4.7 에이전트 실시간 모니터링 대시보드 ✅ 핵심 기능 구현 완료

| 항목 | 내용 |
|------|------|
| **현황** | **핵심 기능 구현 완료 (2026-03-06).** 에이전트별 사용량 추적 + 비정상 상태 자동 감지·알림 구현. |
| **구현 내용** | • **에이전트별 사용량 추적**: `agent_usage_logs` DB 테이블, 태스크 완료 시 자동 기록 (에이전트/태스크/프로바이더/소요시간/로그크기/결과). `GET /api/agent-usage` (요약), `GET /api/agent-usage/:agentId` (상세+일별 집계) API 제공. `CliUsagePanel`에 에이전트별 사용량 접이식 섹션 (실행 횟수, 총 시간, 성공률) 추가.<br>• **비정상 상태 자동 감지**: `agent-anomaly-monitor.ts`에서 60초 주기 스윕. (1) 고아 에이전트 감지 (working 상태 5분 이상 + 프로세스 없음), (2) 연속 실패 감지 (3회 이상). `agent_anomaly` 타입 알림 자동 생성. |
| **구현 위치** | DB: `base-schema.ts` (`agent_usage_logs`), 서버: `ops/agent-usage.ts`, `run-complete-handler.ts` (기록 연동), `agent-anomaly-monitor.ts` (모니터), 프론트: `CliUsagePanel.tsx`, `api/agent-usage.ts` |
| **보완 가능** | • 에이전트별 현재 작업 진행 상황 요약<br>• 시간축 추이 차트 (일별/주별) |

### 4.8 태스크 템플릿·반복 태스크 ✅ 전체 구현 완료

| 항목 | 내용 |
|------|------|
| **현황** | **전체 구현 완료 (2026-03-06).** 템플릿 CRUD + cron 기반 반복 태스크 스케줄링 모두 구현됨. |
| **구현 내용 (템플릿)** | `task_templates` DB 테이블, REST API (GET/POST/DELETE `/api/task-templates`), CreateTaskModal에 템플릿 불러오기 드롭다운 + 현재 폼 저장. |
| **구현 내용 (스케줄러)** | • `scheduled_tasks` DB 테이블: 스케줄 ID, 템플릿 연결, cron 표현식, 자동 실행 옵션, 담당 에이전트/프로젝트 바인딩<br>• 경량 cron 파서: 5필드 표현식 (분/시/일/월/요일), 범위(1-5), 스텝, 리스트, 와일드카드 지원<br>• 60초 주기 스윕 엔진: `next_run_at <= now`인 스케줄 자동 실행, 태스크 생성 + 알림 + 선택적 자동 실행<br>• REST API: CRUD (`/api/scheduled-tasks`), 토글 (`/:id/toggle`), cron 검증 (`/cron/validate`)<br>• 다국어 cron 설명 생성 (en/ko)<br>• 프론트엔드 스케줄러 패널: 스케줄 목록, 생성/수정 폼, cron 프리셋, 실시간 검증, ON/OFF 토글 |
| **구현 위치** | DB: `base-schema.ts`, 엔진: `task-scheduler.ts`, API: `ops/scheduled-tasks.ts`, 프론트: `api/scheduled-tasks.ts`, `scheduled-tasks/ScheduledTasksPanel.tsx`, `Sidebar.tsx` |

### 4.9 다중 프로젝트 동시 운영 UX ✅ 부분 구현

| 항목 | 내용 |
|------|------|
| **현황** | **프로젝트 필터 구현 완료.** 태스크보드 FilterBar에 프로젝트 드롭다운 추가. `getProjects` API로 프로젝트 목록 로드, `filterProject` 상태로 필터링. |
| **구현 위치** | `TaskBoard.tsx` (`filterProject` state, `getProjects` fetch), `FilterBar.tsx` (프로젝트 드롭다운) |
| **보완 가능** | • 프로젝트별 태스크 카운트 배지<br>• 프로젝트 컨텍스트 전환 시 에이전트 자동 재배치 |

### 4.10 백업·복원·데이터 내보내기 ✅ 구현 완료

| 항목 | 내용 |
|------|------|
| **현황** | **구현 완료.** Settings > Data 탭에서 DB 백업 다운로드, 백업 복원(업로드), 태스크 CSV/JSON 내보내기 지원. 복원 시 기존 DB 자동 백업 생성. |
| **구현 위치** | API: `ops/backup.ts` (GET `/api/backup`, POST `/api/backup/restore`, GET `/api/tasks/export`), 프론트: `api/backup.ts`, `settings/DataSettingsTab.tsx`, `SettingsPanel.tsx` |

### 4.11 토큰 비용 관리·지출 한도 ✅ 비용 알림 + 자동 차단 구현 완료

| 항목 | 내용 |
|------|------|
| **현황** | **비용 알림 + 실행 자동 차단 구현 완료 (2026-03-06).** 프로바이더별 사용량 임계값(%) 설정, 초과 시 알림센터로 자동 알림(cost_alert 타입) 생성 + WebSocket 실시간 전송. 한도 초과 시 태스크 실행 자동 차단 추가. |
| **구현 위치** | API: `ops/worktrees-and-usage.ts` (GET/PUT `/api/cost-alerts`, `checkCostAlerts`, `checkCostBlockExecution`, GET `/api/cost-block-check`), `execution-run.ts` (실행 전 비용 체크, 429 응답), 프론트: `api/cost-alerts.ts`, `CliUsagePanel.tsx`, `useAppActions.ts` (cost_limit_exceeded 에러 표시) |
| **구현 내용 (추가)** | • **실행 자동 차단**: 알림 임계값 + 15% = 차단 임계값 (예: 알림 80% → 차단 95%). 임계값 초과 시 `POST /api/tasks/:id/run`이 429 `cost_limit_exceeded` 반환. 프론트에서 사용자 알림 표시.<br>• **에이전트별 추적**: `agent_usage_logs` 테이블로 에이전트-프로바이더별 사용 이력 기록 (§4.7 연동) |
| **보완 가능** | • 일간/월간 비용 추이 차트 |

### 4.12 비디오 프리프로덕션 렌더링 완결

| 항목 | 내용 |
|------|------|
| **현황** | `video_preprod` 팩 정의, ContiUX/샷 리스트/편집 가이드 생성, Remotion 스킬 자동 설치(`video-skill-bootstrap.ts`), 렌더 엔진 게이트(`video-render-engine-gate.ts`) 구현. 그러나 **실제 T2V(Text-to-Video) 렌더링 통합**은 미완. |
| **필요 기능** | • Remotion 기반 실제 mp4 렌더링 파이프라인 자동 실행<br>• 렌더링 결과물 자동 검증 (파일 존재·크기·재생 확인)<br>• 렌더링 실패 시 자동 재시도·에러 리포트 |

### 4.13 일부 메신저 Receiver 미구현

| 항목 | 내용 |
|------|------|
| **현황** | Telegram·Discord는 Receiver(수신) 구현 완료. Signal·iMessage·WhatsApp·Google Chat은 **발신(Send)만** 구현, **수신(Receiver/Webhook)** 미구현. |
| **필요 기능** | • Signal: signal-cli REST API 기반 수신 폴링 또는 웹훅<br>• WhatsApp: Meta Webhook 수신 엔드포인트<br>• Google Chat: Pub/Sub 또는 Bot API 수신<br>• iMessage: macOS 전용, 수신 구현 난이도 높음 (우선순위 낮음) |

### 4.14 Heartbeat — 프로액티브 에이전트 시스템

| 항목 | 내용 |
|------|------|
| **현황** | **✅ 구현 완료 (2026-03-06).** 에이전트별 Heartbeat 설정, 주기적 프로젝트/태스크 점검, HEARTBEAT_OK 패턴, anomaly monitor 통합 모두 구현됨. |
| **경쟁 분석** | OpenClaw의 킬러 피처. 30분 간격 자율 체크, HEARTBEAT.md 체크리스트 기반, 사용자 프롬프트 없이 프로액티브 알림. 247K GitHub 스타의 핵심 인기 요인. |
| **구현 내용** | • `heartbeat_configs` DB 테이블: 에이전트별 활성화/간격/체크항목 설정<br>• `heartbeat_logs` DB 테이블: 점검 이력 (ok/alert/error 상태)<br>• 4가지 체크 항목: `stale_tasks`(정체 태스크), `blocked_tasks`(차단 태스크), `consecutive_failures`(연속 실패), `pending_decisions`(대기 결정)<br>• HEARTBEAT_OK 패턴: 이상 없으면 로그만, 이상 있을 때만 알림센터 + CEO 메신저 통보<br>• REST API: 설정 CRUD (`/api/heartbeat/configs`), 로그 조회 (`/api/heartbeat/logs`), 수동 트리거 (`/api/heartbeat/trigger/:agentId`)<br>• 프론트엔드 HeartbeatPanel: 오피스뷰에 통합, 에이전트 추가/설정/수동 실행/로그 확인 |
| **구현 위치** | `server/modules/workflow/orchestration/heartbeat.ts`, `server/modules/routes/ops/heartbeat.ts`, `src/components/office-view/HeartbeatPanel.tsx`, `src/api/heartbeat.ts` |

### 4.15 Docker 샌드박싱 — 에이전트 실행 격리

| 항목 | 내용 |
|------|------|
| **현황** | **미구현.** worktree 기반 Git 레벨 격리만 존재. 파일 시스템·네트워크·프로세스 수준 격리 없음. |
| **경쟁 분석** | OpenClaw은 Docker 기반 3단계 샌드박싱(off / non-main / all). 위험 경로 자동 차단(`docker.sock`, `/etc`, `/proc`), 네트워크 프록시, workspace 단위 접근 제어. |
| **필요 기능** | • 에이전트 실행 시 Docker 컨테이너 내에서 CLI 프로세스 실행 옵션<br>• 3단계 모드: `off`(기본, 현행), `per-agent`(에이전트별 설정), `all`(전체 격리)<br>• workspace 마운트만 허용, 호스트 위험 경로 차단<br>• `setupCommand`로 컨테이너 초기화 (Node/Python 등 런타임 설치) |
| **구현 방향** | `spawn.ts`에서 Docker exec 분기 추가. Settings에 샌드박스 모드 설정 UI. |

### 4.16 로컬 LLM 지원 (Ollama)

| 항목 | 내용 |
|------|------|
| **현황** | **✅ 구현 완료 (2026-03-06).** `cli_provider`에 `ollama` 추가, Ollama REST API 연동, 자동 감지·프로바이더 등록, 기존 API Provider 경로 재활용 모두 구현됨. |
| **경쟁 분석** | OpenClaw은 Ollama 통해 완전 오프라인 동작 지원. Claude, GPT, DeepSeek, Gemini 외에 로컬 모델 자유 선택. |
| **구현 내용** | • `cli_provider` CHECK 제약에 `ollama` 추가 (agents, rule/memory/hook/skill_learning_history 테이블)<br>• `spawn.ts`, `execution-start-task.ts`에서 `ollama` → `launchApiProviderAgent` 경로 자동 라우팅 (API Provider ID·모델 자동 해석)<br>• `GET /api/ollama/status`: Ollama 로컬 실행 여부 감지 + 모델 목록 조회<br>• `POST /api/ollama/setup`: Ollama API Provider 항목 자동 생성<br>• `api-providers.ts` 프리셋에 `ollama` 타입 추가 (base_url: `http://localhost:11434/v1`)<br>• 프론트엔드 `CliProvider` 타입 + `CLI_PROVIDERS` 배열에 `ollama` 포함 |
| **구현 위치** | DB: `base-schema.ts`, 서버: `spawn.ts`, `execution-start-task.ts`, `api-providers.ts`, 프론트: `types/index.ts`, `agent-manager/constants.ts` |

### 4.17 커뮤니티 스킬 마켓플레이스

| 항목 | 내용 |
|------|------|
| **현황** | **미구현.** 자체 스킬 라이브러리(CRUD, 학습)는 존재하나 로컬 전용. 스킬 내보내기/가져오기/공유 없음. |
| **경쟁 분석** | OpenClaw의 ClawHub: 13,700+ 커뮤니티 스킬, 공개 레지스트리, 버전관리(semver), 벡터 검색, 별점/댓글, CLI 설치(`claw install`). |
| **필요 기능** | • 스킬 패키지 표준 포맷 (SKILL.md 유사, JSON 메타데이터 + 프롬프트 + 지원 파일)<br>• 스킬 내보내기(export) / 가져오기(import) 기능<br>• 장기: 공개 레지스트리 허브 (웹 브라우징 + API) |
| **구현 방향** | 1단계: 스킬 JSON 패키지 export/import. 2단계: GitHub Gist/Repo 기반 공유. 3단계: 전용 레지스트리. |

### 4.18 에이전트 자율 메모리 축적 프로토콜

| 항목 | 내용 |
|------|------|
| **현황** | **✅ 구현 완료 (2026-03-06).** 태스크 완료 시 자동 학습 추출, 태스크 시작 시 관련 메모리 자동 검색·프롬프트 주입, 중복 방지 모두 구현됨. |
| **경쟁 분석** | OpenClaw의 MEMORY PROTOCOL: 자동 캡처(대화·작업 중 중요 정보 자동 저장), 검색 가능한 영속 메모리, Heartbeat 연동 자동 유지보수. |
| **구현 내용** | • 태스크 완료 시 자동 학습 추출: 에러 패턴(실패 시), 인사이트/TODO/파일 변경(성공 시) → `memory_entries` DB에 자동 저장<br>• 태스크 시작 시 관련 메모리 자동 검색: 에이전트/부서/팩/글로벌 스코프 매칭 + 키워드 관련도 스코어링 → 상위 10개 프롬프트 주입<br>• 중복 방지: 동일 제목 메모리 존재 시 스킵<br>• 자동 저장 메모리는 `[Auto]` 접두사로 구분, CEO가 수동 관리도 가능 |
| **구현 위치** | `server/modules/workflow/orchestration/autonomous-memory.ts` (검색·추출 엔진), `execution-start-task.ts` (시작 시 주입), `run-complete-handler.ts` (완료 시 추출) |

---

## 5. 경쟁 분석: OpenClaw 대비 포지셔닝

> **갱신: 2026-03-06** — OpenClaw(247K GitHub 스타, MIT 라이선스) 대비 AgentDesk의 강점과 격차 분석.

### 5.1 AgentDesk 고유 강점 (OpenClaw에 없는 것)

| 영역 | AgentDesk 강점 | 비고 |
|------|---------------|------|
| **멀티 에이전트 팀 운영** | CEO가 부서·에이전트 팀을 조직적으로 지휘. 역할(team_leader/senior/junior/intern), 부서 배정, 크로스부서 협업 | OpenClaw은 단일 에이전트 |
| **비주얼 오피스 시뮬레이터** | 픽셀아트 오피스, 부서별 룸, 에이전트 이동 시각화 | OpenClaw은 CLI/메시징 전용 |
| **태스크·프로젝트 관리** | 칸반 보드, 의존성, 템플릿, 서브태스크, worktree 격리, 프로젝트 CRUD, GitHub 임포트 | OpenClaw은 단순 채팅 기반 |
| **Workflow Pack** | 7개 도메인별 실행 분기 (개발, 소설, 리포트, 영상, 웹리서치, 롤플레이, 자산관리) | OpenClaw은 범용 스킬 기반 |
| **의사결정 워크플로우** | Decision Inbox, CEO 승인/거부, YOLO 모드 | OpenClaw은 자율 실행 위주 |
| **캐릭터 페르소나** | 에이전트별 AI 성격 자동 생성·7개 지점 주입 | OpenClaw에 해당 기능 없음 |
| **데스크톱 앱** | Electron EXE 패키징, 자동 업데이트, 네이티브 파일 피커 | OpenClaw은 Docker/VPS 기반 |
| **결과물 관리** | Deliverables 전용 화면, diff 뷰, merge/discard, 리포트 아카이브 | OpenClaw은 파일 기반 산출물만 |

### 5.2 OpenClaw 대비 격차 (신규 필요 기능)

| 격차 영역 | OpenClaw | AgentDesk 현황 | 중요도 | 관련 섹션 |
|-----------|----------|---------------|--------|-----------|
| **Heartbeat (프로액티브 에이전트)** | 30분 자율 체크, HEARTBEAT.md, 프롬프트 없이 알림 | 미구현. 태스크 할당 시에만 동작 | **높음** | §4.14 |
| **Docker 샌드박싱** | 3단계 격리, 위험 경로 차단, 네트워크 프록시 | worktree 격리만 (Git 레벨) | **높음** | §4.15 |
| **커뮤니티 스킬 마켓플레이스** | ClawHub 13,700+ 스킬, 공개 레지스트리 | 로컬 스킬 라이브러리만 | **중간** | §4.17 |
| ~~**로컬 LLM (Ollama)**~~ | ~~완전 오프라인 동작 가능~~ | ✅ 구현 완료. `ollama` cli_provider + 자동 감지 | ~~**중간**~~ | §4.16 |
| **메시징 수신 완전 지원** | WhatsApp(Baileys), Signal 등 전 플랫폼 양방향 | Telegram·Discord만 수신 | **중간** | §4.13 |
| **자율 메모리 프로토콜** | 자동 캡처·검색·유지보수 | 수동 CRUD만, 자율 축적 없음 | **중간** | §4.18 |

---

## 6. 운영·보안 보완계획 (문서 기준)

> 이하 섹션은 §5(경쟁 분석) 이후 번호입니다.

다음은 **운영/인프라 보안** 문서에서 정리한 "보완 필요" 항목입니다. 제품 기능보다 **프로세스·배포·보안** 쪽에 가깝습니다.

### 5.1 운영 (docs/operations)

| 항목 | 내용 |
|------|------|
| **파이프라인 게이트 + SLA** | ✅ 구현 완료. `pipeline_gates` 테이블 기반 워크플로우팩별 게이트 정의 + 자동/수동 게이트 + SLA 설정 + TaskCard UI |
| **자동 트래킹** | ✅ `task_gate_results` 테이블로 게이트 평가 결과 기록. 자동 게이트는 run-complete-handler에서 평가 |
| **블로커 에스컬레이션** | 30분/60분 규칙, 모니터링 로그 30분 간격 업데이트 |

### 5.2 인프라 보안 (docs/security)

| 항목 | 내용 |
|------|------|
| **무결성 검증** | 배포 산출물 SHA-256 기준선·버전 고정, 배포 전 재검증 |
| **RBAC** | PPT/협업 저장소 권한 최소화 점검(icacls), 기준 충족 시 배포 허용 |
| **CI 보안 게이트** | 비밀정보 0건, 악성 매크로/첨부 검사, 접근권한 변경 이력 제출 |

---

## 7. 기술 부채·리팩터 계획

| 항목 | 상태 | 요약 |
|------|------|------|
| `@ts-nocheck` 제거 | ✅ **완료** | 서버 코드에서 `@ts-nocheck` 0건 확인 (실사 완료) |
| 공통 타입 정리 | ⚠️ 진행 중 | `server/modules/*/shared` 패턴은 아직 미적용, 타입 일부 인라인 선언 잔존 |
| `buildDocumentGenerationGuidance` 함수명 | ✅ **완료** | `buildPptExecutionGuidance` → `buildDocumentGenerationGuidance` rename + 파일명 변경 완료 |

---

## 8. 필요 기능 우선순위 제안

> **갱신: 2026-03-06** — P0~P1 전체 구현 완료. OpenClaw 경쟁 분석(§5) 반영하여 신규 항목 추가 + P1~P3 재분류.

### 완료된 항목 (아카이브)

| 우선순위 | 영역 | 기능 | 비고 |
|----------|------|------|------|
| ~~P0~~ | 연동 | Decision Inbox → 메신저 wake 알림 | ✅ 구현 완료 |
| ~~P0~~ | 결과물 | 산출물 파일 다운로드 API·UI | ✅ 구현 완료 |
| ~~P1~~ | UX | 알림 센터 (웹 UI 내 통합 알림) | ✅ 구현 완료 |
| ~~P1~~ | 확장 | Workflow Pack 팩별 입력 폼 | ✅ 구현 완료 |
| ~~P1~~ | 분석 | 에이전트 성과 히스토리 | ✅ 구현 완료 |
| ~~P2~~ | UX | 태스크 템플릿 | ✅ 구현 완료 |
| ~~P2~~ | UX | 태스크보드 프로젝트 필터 | ✅ 구현 완료 |
| ~~P2~~ | UX | 터미널 로그 검색·필터·내보내기 | ✅ 구현 완료 |
| ~~P2~~ | 학습 UI | 룰/메모리/훅 낙관적 업데이트 | ✅ 구현 완료 |
| ~~P2~~ | 비용 | 토큰 비용 관리·비용 알림 MVP | ✅ 구현 완료 |
| ~~P3~~ | 시각화 | 프로젝트 진행률·에이전트 기여도 | ✅ 구현 완료 |
| ~~P3~~ | 시각화 | 태스크 의존성 MVP | ✅ 구현 완료 |
| ~~P3~~ | 운영 | 백업·복원·데이터 내보내기 | ✅ 구현 완료 |

### ~~현재 우선순위 — P1 (높음: 운영 안정성·비용 관리)~~ ✅ 전체 구현 완료 (2026-03-06)

| # | 영역 | 기능 | 상태 | 관련 섹션 |
|---|------|------|------|-----------|
| ~~1~~ | ~~모니터링~~ | ~~에이전트별 토큰 소비량·비용 실시간 추적~~ | ✅ 구현 완료. `agent_usage_logs` DB + API + CliUsagePanel 에이전트 섹션 | §4.7, §4.11 |
| ~~2~~ | ~~모니터링~~ | ~~비정상 상태 자동 감지·알림~~ | ✅ 구현 완료. `agent-anomaly-monitor.ts` 60초 스윕 (고아 에이전트 + 연속 실패) | §4.7 |
| ~~3~~ | ~~Workflow~~ | ~~팩별 QA 게이트 런타임 자동 적용~~ | ✅ 구현 완료. `execution-guidance.ts` 다국어 QA 프롬프트 주입 + `run-complete-handler.ts` 완료 시 검증 | §3.3 |
| ~~4~~ | ~~비용~~ | ~~한도 초과 시 실행 자동 차단~~ | ✅ 구현 완료. `checkCostBlockExecution()` + 실행 전 429 차단 + 프론트 알림 | §4.11 |

### ~~현재 우선순위 — P1 신규 (경쟁 격차 해소·차별화)~~ 부분 구현 완료 (2026-03-06)

| # | 영역 | 기능 | 상태 | 관련 섹션 |
|---|------|------|------|-----------|
| ~~5~~ | ~~에이전트~~ | ~~Heartbeat — 프로액티브 에이전트 시스템~~ | ✅ 구현 완료. `heartbeat.ts` 엔진 + API + HeartbeatPanel UI. 4가지 체크항목, HEARTBEAT_OK 패턴, 수동 트리거 | §4.14 |
| 6 | **보안** | **Docker 샌드박싱 (에이전트 실행 격리)** | ⏸️ 보류 (사용자 요청) | §4.15 |
| ~~7~~ | ~~에이전트~~ | ~~자율 메모리 축적 프로토콜~~ | ✅ 구현 완료. `autonomous-memory.ts` 엔진. 태스크 시작 시 자동 검색·주입, 완료 시 자동 학습 추출 | §4.18 |

### 현재 우선순위 — P2 (중간: 워크플로우 완성·시각화·확장)

| # | 영역 | 기능 | 근거 | 관련 섹션 |
|---|------|------|------|-----------|
| ~~8~~ | ~~에이전트~~ | ~~Ollama/로컬 LLM 프로바이더 지원~~ | ✅ 구현 완료. `ollama` cli_provider + 자동 감지/등록 + API Provider 경로 재활용 | §4.16 |
| ~~9~~ | ~~UX~~ | ~~반복 태스크 스케줄링 (cron 기반)~~ | ✅ 구현 완료. `scheduled_tasks` DB + cron 파서 + 60초 스윕 엔진 + CRUD API + 스케줄러 UI 패널 | §4.8 |
| 10 | 연동 | WhatsApp Receiver (Baileys/Meta Webhook) | 사용자 수 기준 최우선 메신저 확장. OpenClaw은 이미 구현 | §4.13 | -- 보류
| ~~11~~ | ~~시각화~~ | ~~간트 차트 / 타임라인 뷰~~ | ✅ 구현 완료. TaskBoard 내 Board/Gantt 뷰 전환. 줌(일/주/월), 상태/부서 필터, 오늘 마커, 상태별 색상 바 | §4.6 |
| ~~12~~ | ~~시각화~~ | ~~시간축 번다운/번업 차트~~ | ✅ 구현 완료. SVG 기반 번다운 차트. `GET /api/projects/:id/burndown` API + ProjectInsightsPanel 통합 | §4.4 |
| ~~13~~ | ~~시각화~~ | ~~에이전트 일별/주별 비용 추이 차트~~ | ✅ 구현 완료. 프로바이더별 스택 바 차트 (7/14/30일). `GET /api/agent-usage/trends/daily` API + CliUsagePanel 통합 | §4.11 |
| ~~14~~ | ~~결과물~~ | ~~파일 인라인 미리보기 (이미지·PDF)~~ | ✅ 구현 완료. `ArtifactList.tsx` 토글 기반 이미지/PDF 인라인 프리뷰 | §4.1, §3.1 |
| ~~15~~ | ~~Workflow~~ | ~~세션/메신저별 기본 팩 자동 라우팅 UI 노출~~ | ✅ 구현 완료. 프로젝트 편집기에 기본 워크플로우 팩 선택 UI 추가. 세션별 팩은 Gateway 설정에서 이미 노출 | §3.3 |
| ~~16~~ | ~~UX~~ | ~~의존성 완료 전 실행 자동 차단~~ | ✅ 구현 완료. `startTaskExecutionForAgent`에서 선행 태스크 미완료 시 실행 차단 + CEO 알림. `GET /api/tasks/:id/dependencies/blocked` API 추가 | §4.6 |

### 현재 우선순위 — P3 (낮음: 장기·니치·생태계)

| # | 영역 | 기능 | 근거 | 관련 섹션 |
|---|------|------|------|-----------|
| ~~17~~ | ~~생태계~~ | ~~커뮤니티 스킬 마켓플레이스 (1단계: export/import)~~ | ✅ 1단계 구현 완료. `GET /api/skills/custom/:name/export` + `POST /api/skills/custom/import` API. 프론트엔드 Export/Import 버튼 | §4.17 |
| 18 | 영상 | 비디오 프리프로덕션 T2V 렌더링 통합 | Remotion 기반 mp4 파이프라인. 복잡도 높음 | §4.12 |
| 19 | 연동 | Signal Receiver (signal-cli REST API 폴링) | 사용 빈도 낮음 | §4.13 |  -- 보류
| 20 | 연동 | Google Chat Receiver (Pub/Sub) | 사용 빈도 낮음 | §4.13 | -- 보류
| 21 | 연동 | iMessage Receiver | macOS 전용, 난이도 최상 | §4.13 | -- 보류
| ~~22~~ | ~~운영/보안~~ | ~~파이프라인 게이트·SLA·보안 게이트~~ | ✅ 구현 완료. `pipeline_gates`+`task_gate_results` DB 테이블, 워크플로우팩별 게이트 시드, 자동 게이트 평가(run-complete-handler), 수동 승인/반려 API, TaskCard UI 통합 | §6 |
| 23 | 기술 | 공통 타입 정리 (`server/modules/*/shared` 패턴) | 유지보수성 개선 | §7 |
| ~~24~~ | ~~기술~~ | ~~`buildPptExecutionGuidance` → `buildDocumentGenerationGuidance` 함수명 개선~~ | ✅ 구현 완료. 함수명·파일명 모두 rename 완료 | §7 |
| ~~25~~ | ~~UX~~ | ~~브라우저 Push Notification (Notification API)~~ | ✅ 구현 완료. `NotificationCenter`에서 Notification API 연동. 창 비활성 시 자동 알림. 토글 버튼 | §4.5 |
| ~~26~~ | ~~UX~~ | ~~알림 유형별 필터~~ | ✅ 구현 완료. All/✅/❌/📬/⚠️/ℹ️ 필터 버튼 추가 | §4.5 |
| ~~27~~ | ~~분석~~ | ~~프로바이더별 성공/실패 분석~~ | ✅ 구현 완료. CliUsagePanel에 프로바이더별 성공률 바 + 성공/실패 카운트 위젯 추가 | §4.2 |
| ~~28~~ | ~~UX~~ | ~~프로젝트별 태스크 카운트 배지~~ | ✅ 구현 완료. ProjectSidebar에 task_count 배지 표시 | §4.9 |
| ~~29~~ | ~~UX~~ | ~~학습됨 상태 버튼 문구/스타일 구분~~ | ✅ 구현 완료. SkillsGrid/HooksGrid/MemoryGrid에 학습 상태별 버튼 문구·스타일·툴팁 차별화 적용 (AgentRulesGrid 패턴 통일) | §3.2 |
| ~~30~~ | ~~시각화~~ | ~~부서별 기여도 분류~~ | ✅ 구현 완료. ProjectInsightsPanel에 부서별 기여도 바 차트 추가. 서버 API에 department_name 포함 | §4.4 |

### 추천 구현 로드맵

```
1단계 ✅ 완료 (2026-03-06) ─ 모니터링·비용 관리
  → 에이전트별 토큰/비용 추적 + 비정상 감지 + 비용 자동 차단 + QA 게이트

2단계 ✅ 부분 완료 (2026-03-06) ─ 프로액티브 에이전트
  → Heartbeat 시스템 ✅ + 자율 메모리 축적 프로토콜 ✅
  → Docker 샌드박싱 ⏸️ 보류

3단계 (P2 전반) ─ 워크플로우 완성 · 확장
  → Ollama/로컬 LLM ✅ + 반복 태스크 스케줄링 ✅ + WhatsApp Receiver
  → 팩 자동 라우팅 UI ✅ + 의존성 실행 차단 ✅

4단계 (P2 후반) ─ 시각화·UX 고도화
  → 간트 차트 ✅ + 번다운 차트 ✅ + 비용 추이 차트 ✅ + 파일 인라인 미리보기 ✅

5단계 (P3) ─ 장기 과제
  → 스킬 마켓플레이스 · 비디오 렌더링 · Signal/Google Chat
  → 운영보안 게이트 · 기술 부채
```

---

## 9. 참고 문서

| 문서 | 용도 |
|------|------|
| `README_ko.md` | 개요·주요 기능·요구사항 |
| `docs/architecture/README.md` | 시스템 개요·API·DB·부서 구조 |
| `docs/architecture/CEO-STRUCTURE-MAP.md` | CEO 구조 맵 |
| `docs/DESIGN.md` | UI/UX·컬러·컴포넌트 가이드 |
| `docs/DESIGN_SKILLS.md` | 스킬 학습 이력·CLI 프롬프트 디자인 |
| `docs/api.md` | API 계약·메신저 세션·인증 |
| `docs/deliverables-spec.md` | 결과물 화면 상세 스펙 |
| `docs/agent-rules-learning-ui-analysis.md` | 룰 학습 UI 개선 방향 |
| `docs/plans/2026-02-27-workflow-pack-mvp.md` | Workflow Pack MVP 범위·모델 |
| `docs/plans/2026-02-25-server-types-nocheck-removal.md` | 서버 ts-nocheck 제거 계획 |
| `docs/decision-inbox-checkpoint-2026-02-22.md` | 의사결정 Inbox·다음 작업 |
| `docs/exe-packaging-plan.md` | EXE 빌드·문제 해결 |
| `docs/operations/2026-03-05-operations-runbook.md` | 운영 게이트·SLA |
| `docs/operations/2026-03-05-monitoring-log.md` | 모니터링 로그 |
| `docs/security/2026-03-05-infra-security-plan.md` | 보안 보완계획 |
| `docs/security/2026-03-05-security-gate-report.md` | 보안 게이트 실행 결과 |
| `docs/factcheck/2026-03-05-factcheck-report.md` | 팩트체크 산출물 |

---

*이 문서는 코드베이스 실사(2026-03-05) + P0/P1/P2/P3 구현 반영 기준으로 갱신되었습니다.*
*2026-03-06: P1 전체 구현 완료 반영 (에이전트별 사용량 추적, 비정상 감지, QA 게이트, 비용 자동 차단).*
*2026-03-06: OpenClaw 경쟁 분석 추가 (§5). Heartbeat·Docker 샌드박싱·로컬 LLM·커뮤니티 스킬·자율 메모리 등 신규 필요 기능 5건 추가 (§4.14~§4.18). 우선순위 재분류 (§8).*
